import React from "react";
import ToggleColorMode from "./../toggleColorMode";

const Layout = () => {
  return (
    <div>
      <ToggleColorMode />
      <div>Layout</div>
      <div>Layout</div>
      <div>Layout</div>
      <div>Layout</div>
    </div>
  );
};

export default Layout;
